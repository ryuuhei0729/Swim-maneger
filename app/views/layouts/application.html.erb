<!DOCTYPE html>
<html>
  <head>
    <title>Swim Manager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gray-50">
    <div class="min-h-screen flex flex-col">
      <% if user_auth_signed_in? %>
        <nav class="bg-white shadow-sm">
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
              <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                  <%= link_to "Swim Manager", root_path, class: "text-xl font-bold text-blue-600" %>
                </div>
              </div>
              <div class="flex items-center">
                <div class="ml-3 relative">
                  <div class="flex items-center space-x-4">
                    <span class="text-gray-700"><%= current_user_auth.email %></span>
                    <%= button_to "ログアウト", destroy_user_auth_session_path, method: :delete, class: "text-gray-600 hover:text-gray-900" %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </nav>
      <% end %>

      <main class="flex-grow">
        <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
          <% if notice %>
            <div class="mb-4 p-4 bg-green-50 border border-green-200 text-green-700 rounded-lg">
              <%= notice %>
            </div>
          <% end %>
          <% if alert %>
            <div class="mb-4 p-4 bg-red-50 border border-red-200 text-red-700 rounded-lg">
              <%= alert %>
            </div>
          <% end %>

          <%= yield %>
        </div>
      </main>

      <footer class="bg-white shadow-sm mt-auto">
        <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8">
          <p class="text-center text-gray-500 text-sm">
            &copy; <%= Time.current.year %> Swim Manager. All rights reserved.
          </p>
        </div>
      </footer>
    </div>
  </body>
</html>
