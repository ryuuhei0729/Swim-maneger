<!DOCTYPE html>
<html>
  <head>
    <title>SwimManager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gray-50">
    <% if flash[:needs_reload] %>
      <script>
        if (sessionStorage.getItem('reloaded_after_login') !== 'true') {
          sessionStorage.setItem('reloaded_after_login', 'true');
          window.location.reload();
        }
      </script>
    <% end %>
    <% if user_auth_signed_in? %>
      <div class="flex min-h-screen">
        <!-- サイドバー -->
        <%= render 'shared/sidebar' %> 

        <!-- メインコンテンツエリア -->
        <div class="flex-1 flex flex-col md:pl-64 w-full">
          <!-- ヘッダー -->
          <%= render 'shared/header' %>

          <!-- メインコンテンツ -->
          <main class="flex-1 overflow-y-auto bg-gray-50 relative">
            <div class="max-w-full px-4 sm:px-6 lg:px-8 py-8">
              <!-- ここにフラッシュメッセージを表示（成功や失敗など） -->
              <% if notice %>
                <div class="mb-4 p-4 bg-green-50 border border-green-200 text-green-700 rounded-lg">
                  <%= notice %>
                </div>
              <% end %>
              <% if alert %>
                <div class="mb-4 p-4 bg-red-50 border border-red-200 text-red-700 rounded-lg">
                  <%= alert %>
                </div>
              <% end %>
              
              <%= yield %>


            </div>
          </main>
        </div>
      </div>
    <% else %>
      <%= yield %>
    <% end %>
  </body>
</html>
