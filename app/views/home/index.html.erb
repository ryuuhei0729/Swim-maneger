<div class="bg-white shadow rounded-lg p-6">
  <div class="text-center mb-8">
    <% if current_user %>
      <h1 class="text-3xl font-bold text-gray-900 mb-2">ようこそ、<%= current_user.name %>さん</h1>
      <p class="text-gray-600">Swim Managerへようこそ！</p>
    <% else %>
      <h1 class="text-3xl font-bold text-gray-900 mb-2">Swim Manager</h1>
      <p class="text-gray-600">水泳部の活動をもっと楽しく、効率的に。</p>
      <div class="mt-8 flex justify-center space-x-4">
        <%= link_to "ログイン", new_user_auth_session_path, class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        <%= link_to "新規登録", new_user_auth_registration_path, class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      </div>
    <% end %>
  </div>

  <% if current_user %>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div class="bg-blue-50 p-6 rounded-lg">
        <h2 class="text-xl font-semibold text-blue-900 mb-4">プロフィール情報</h2>
        <div class="space-y-3">
          <div class="flex justify-between">
            <span class="text-gray-600">名前</span>
            <span class="font-medium"><%= current_user.name %></span>
          </div>
          <div class="flex justify-between">
            <span class="text-gray-600">メールアドレス</span>
            <span class="font-medium"><%= current_user.email %></span>
          </div>
          <div class="flex justify-between">
            <span class="text-gray-600">世代</span>
            <span class="font-medium"><%= current_user.generation %>期生</span>
          </div>
          <div class="flex justify-between">
            <span class="text-gray-600">性別</span>
            <span class="font-medium"><%= current_user.gender == 'male' ? '男性' : '女性' %></span>
          </div>
          <div class="flex justify-between">
            <span class="text-gray-600">生年月日</span>
            <span class="font-medium"><%= current_user.birthday.strftime('%Y年%m月%d日') %></span>
          </div>
        </div>
      </div>

      <div class="bg-green-50 p-6 rounded-lg">
        <h2 class="text-xl font-semibold text-green-900 mb-4">プロフィール画像</h2>
        <div class="flex justify-center">
          <% if current_user.profile_image_url.present? %>
            <img src="<%= current_user.profile_image_url %>" alt="プロフィール画像" class="w-32 h-32 rounded-full object-cover shadow-lg">
          <% else %>
            <div class="w-32 h-32 rounded-full bg-gray-200 flex items-center justify-center">
              <span class="text-gray-400">No Image</span>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <% if current_user.bio.present? %>
      <div class="mt-8 bg-gray-50 p-6 rounded-lg">
        <h2 class="text-xl font-semibold text-gray-900 mb-4">自己紹介</h2>
        <p class="text-gray-700 whitespace-pre-wrap"><%= current_user.bio %></p>
      </div>
    <% end %>
  <% else %>
    <div class="mt-8 bg-gray-50 p-6 rounded-lg">
      <h2 class="text-xl font-semibold text-gray-900 mb-4">Swim Managerとは？</h2>
      <p class="text-gray-700">
        Swim Managerは、水泳部の活動をより楽しく、効率的にするための管理ツールです。
        メンバー同士のコミュニケーションを促進し、練習記録の管理や大会情報の共有を簡単に行うことができます。
      </p>
      <div class="mt-4 space-y-3">
        <div class="flex items-start">
          <div class="flex-shrink-0">
            <svg class="h-6 w-6 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <p class="ml-3 text-gray-700">練習記録の管理と分析</p>
        </div>
        <div class="flex items-start">
          <div class="flex-shrink-0">
            <svg class="h-6 w-6 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <p class="ml-3 text-gray-700">大会情報の共有とエントリー管理</p>
        </div>
        <div class="flex items-start">
          <div class="flex-shrink-0">
            <svg class="h-6 w-6 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <p class="ml-3 text-gray-700">メンバー間のコミュニケーション促進</p>
        </div>
      </div>
    </div>
  <% end %>
</div>
