<% content_for :title, 'Home' %>
<div class="w-full pt-8">
  <div class="text-center mb-12">
    <h1 class="text-4xl font-bold text-gray-900 mb-3">ようこそ <%= current_user_auth.user.name %> さん</h1>
    <p class="text-lg text-gray-600">Swim Managerへようこそ！</p>
  </div>

  <!-- お知らせセクション -->
  <div class="bg-white rounded-2xl shadow-xl overflow-hidden mb-12">
    <div class="flex items-center justify-between p-6 bg-blue-800">
      <h2 class="text-2xl font-bold text-white">お知らせ</h2>
    </div>

    <div class="p-6">
      <div class="space-y-6">
        <% if @birthday_users.any? %>
          <div class="border-l-4 border-pink-500 pl-4 py-2">
            <div class="flex items-start">
              <div class="flex-shrink-0 mt-1">
                <svg class="h-5 w-5 text-pink-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 15.546c-.523 0-1.046.151-1.5.454a2.704 2.704 0 01-3 0 2.704 2.704 0 00-3 0 2.704 2.704 0 01-3 0 2.704 2.704 0 00-3 0 2.701 2.701 0 00-1.5-.454M9 6v2m3-2v2m3-2v2M9 3h.01M12 3h.01M15 3h.01M21 21v-7a2 2 0 00-2-2H5a2 2 0 00-2 2v7h18zm-3-9v-2a2 2 0 00-2-2H8a2 2 0 00-2 2v2h12z" />
                </svg>
              </div>
              <div class="ml-3">
                <h3 class="text-lg font-medium text-gray-900">
                  <% @birthday_users.each do |user| %>
                    <span class="text-pink-500"><%= user.name %></span>さん、
                  <% end %>
                  お誕生日おめでとうございます！
                </h3>
              </div>
            </div>
          </div>
        <% end %>

        <% if @announcements.any? %>
          <% @announcements.each do |announcement| %>
            <div class="border-l-4 border-blue-500 pl-4 py-2">
              <div class="flex items-start">
                <div class="flex-shrink-0 mt-1">
                  <svg class="h-5 w-5 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                </div>
                <div class="ml-3">
                  <h3 class="text-lg font-medium text-gray-900 flex items-center">
                    <%= announcement.title %>
                    <% if announcement.published_at >= 3.days.ago %>
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800 ml-2">
                        New!!
                      </span>
                    <% end %>
                  </h3>
                  <p class="mt-1 text-gray-600"><%= announcement.content %></p>
                  <p class="mt-1 text-sm text-gray-500"><%= announcement.published_at.strftime("%Y/%m/%d (#{%w(日 月 火 水 木 金 土)[announcement.published_at.wday]}) %H:%M") %> 更新</p>
                </div>
              </div>
            </div>
          <% end %>
        <% elsif !@birthday_users.any? %>
          <div class="text-center py-4">
            <p class="text-gray-500">現在、お知らせはありません。</p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  
  <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
      <h2 class="text-2xl font-bold ">練習カレンダー</h2>

    <%= render 'shared/calendar', locals: {
      current_month: @current_month,
      events_by_date: @events_by_date,
      birthdays_by_date: @birthdays_by_date,
      user_attendance_by_event: @user_attendance_by_event
    } %>
  </div>
</div>

<!-- ベストタイムセクション -->
<div class="bg-white shadow rounded-lg p-6 mt-12">
  <div class="flex items-center justify-between mb-6">
    <h2 class="text-2xl font-bold text-gray-900">ベストタイム</h2>
  </div>

  <div class="mb-4 border-b border-gray-200">
    <div class="flex items-center justify-between">
      <ul class="flex flex-wrap -mb-px text-sm font-medium text-center" id="genderTabs" role="tablist">
        <li class="mr-2" role="presentation">
          <button class="inline-block p-4 border-b-2 rounded-t-lg <%= @default_tab == 'all' ? 'border-blue-600 text-blue-600 bg-blue-100' : 'border-transparent hover:text-gray-600 hover:border-gray-300' %>" id="all-tab" data-tabs-target="#all" type="button" role="tab" aria-controls="all" aria-selected="<%= @default_tab == 'all' %>">全体</button>
        </li>
        <li class="mr-2" role="presentation">
          <button class="inline-block p-4 border-b-2 rounded-t-lg <%= @default_tab == 'male' ? 'border-blue-600 text-blue-600 bg-blue-100' : 'border-transparent hover:text-gray-600 hover:border-gray-300' %>" id="male-tab" data-tabs-target="#male" type="button" role="tab" aria-controls="male" aria-selected="<%= @default_tab == 'male' %>">男子</button>
        </li>
        <li class="mr-2" role="presentation">
          <button class="inline-block p-4 border-b-2 rounded-t-lg <%= @default_tab == 'female' ? 'border-blue-600 text-blue-600 bg-blue-100' : 'border-transparent hover:text-gray-600 hover:border-gray-300' %>" id="female-tab" data-tabs-target="#female" type="button" role="tab" aria-controls="female" aria-selected="<%= @default_tab == 'female' %>">女子</button>
        </li>
      </ul>
      <div class="text-sm text-red-600">
        <p>※種目をクリックすると、タイム順（昇順）に並び替えられます</p>
      </div>
    </div>
  </div>

  <div id="genderTabContent">
    <div class="<%= @default_tab == 'all' ? '' : 'hidden' %> p-4 rounded-lg bg-gray-50" id="all" role="tabpanel" aria-labelledby="all-tab">
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200 text-xs">
          <thead>
            <tr>
              <th class="px-2 py-2 bg-gray-100 text-left font-medium text-gray-900 uppercase tracking-wider sticky left-0 z-10" rowspan="2">世代</th>
              <th class="px-2 py-2 bg-gray-100 text-left font-medium text-gray-900 uppercase tracking-wider sticky left-0 z-10" rowspan="2">選手名</th>
              <% @events.group_by { |e| e[:style] }.each do |style, events| %>
                <th class="px-2 py-2 text-center font-medium text-gray-900 uppercase tracking-wider" colspan="<%= events.size %>"
                    style="background-color: <%= case style
                      when 'fr' then '#fde047'  # yellow-200
                      when 'br' then '#86efac'  # green-200
                      when 'ba' then '#fca5a5'  # red-200
                      when 'fly' then '#93c5fd'  # blue-200
                      when 'im' then '#d8b4fe'  # purple-200
                      else '#f3f4f6'  # gray-100
                    end %>">
                  <%= case style
                    when 'fr' then '自由形'
                    when 'br' then '平泳ぎ'
                    when 'ba' then '背泳ぎ'
                    when 'fly' then 'バタフライ'
                    when 'im' then '個人メドレー'
                    else style
                  end %>
                </th>
              <% end %>
            </tr>
            <tr>
              <% @events.each do |event| %>
                <th class="px-2 py-2 text-center font-medium text-gray-900 tracking-wider cursor-pointer hover:bg-gray-200"
                    style="background-color: <%= case event[:style]
                      when 'fr' then '#fde047'  # yellow-200
                      when 'br' then '#86efac'  # green-200
                      when 'ba' then '#fca5a5'  # red-200
                      when 'fly' then '#93c5fd'  # blue-200
                      when 'im' then '#d8b4fe'  # purple-200
                      else '#f3f4f6'  # gray-100
                    end %>"
                    data-sort-by="<%= event[:id] %>">
                  <%= "#{event[:distance]}m" %>
                  <% if @sort_by == event[:id] %>
                    <span class="ml-1">↑</span>
                  <% end %>
                </th>
              <% end %>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% if @sort_by.present? %>
              <% @players.each do |player| %>
                <tr class="<%= 'bg-blue-100' if player.id == current_user_auth.user.id %>">
                  <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                    <%= player.generation %>期
                  </td>
                  <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                    <%= player.name %>
                  </td>
                  <% @events.each do |event| %>
                    <td class="px-2 py-2 whitespace-nowrap text-center"
                        style="background-color: <%= player.id == current_user_auth.user.id ? '#dbeafe' : case event[:style]
                          when 'fr' then '#fefce8'  # yellow-50
                          when 'br' then '#f0fdf4'  # green-50
                          when 'ba' then '#fef2f2'  # red-50
                          when 'fly' then '#eff6ff'  # blue-50
                          when 'im' then '#faf5ff'  # purple-50
                          else '#f3f4f6'  # gray-100
                        end %>">
                      <% time = @best_times[player.id][event[:id]] %>
                      <% if time %>
                        <%= format_time(time) %>
                      <% else %>
                        -
                      <% end %>
                    </td>
                  <% end %>
                </tr>
              <% end %>
            <% else %>
              <% @players_by_generation.each do |generation, players| %>
                <% players.each do |player| %>
                  <tr class="hover:bg-gray-50 <%= 'bg-blue-100' if player.id == current_user_auth.user.id %>">
                    <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                      <%= generation %>期
                    </td>
                    <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                      <%= player.name %>
                    </td>
                    <% @events.each do |event| %>
                      <td class="px-2 py-2 whitespace-nowrap text-center"
                          style="background-color: <%= player.id == current_user_auth.user.id ? '#dbeafe' : case event[:style]
                            when 'fr' then '#fefce8'  # yellow-50
                            when 'br' then '#f0fdf4'  # green-50
                            when 'ba' then '#fef2f2'  # red-50
                            when 'fly' then '#eff6ff'  # blue-50
                            when 'im' then '#faf5ff'  # purple-50
                            else '#f3f4f6'  # gray-100
                          end %>">
                        <% time = @best_times[player.id][event[:id]] %>
                        <% if time %>
                          <%= format_time(time) %>
                        <% else %>
                          -
                        <% end %>
                      </td>
                    <% end %>
                  </tr>
                <% end %>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>

    <div class="<%= @default_tab == 'male' ? '' : 'hidden' %> p-4 rounded-lg bg-gray-50" id="male" role="tabpanel" aria-labelledby="male-tab">
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200 text-xs">
          <thead>
            <tr>
              <th class="px-2 py-2 bg-gray-100 text-left font-medium text-gray-900 uppercase tracking-wider sticky left-0 z-10" rowspan="2">世代</th>
              <th class="px-2 py-2 bg-gray-100 text-left font-medium text-gray-900 uppercase tracking-wider sticky left-0 z-10" rowspan="2">選手名</th>
              <% @events.group_by { |e| e[:style] }.each do |style, events| %>
                <th class="px-2 py-2 text-center font-medium text-gray-900 uppercase tracking-wider" colspan="<%= events.size %>"
                    style="background-color: <%= case style
                      when 'fr' then '#fde047'  # yellow-200
                      when 'br' then '#86efac'  # green-200
                      when 'ba' then '#fca5a5'  # red-200
                      when 'fly' then '#93c5fd'  # blue-200
                      when 'im' then '#d8b4fe'  # purple-200
                      else '#f3f4f6'  # gray-100
                    end %>">
                  <%= case style
                    when 'fr' then '自由形'
                    when 'br' then '平泳ぎ'
                    when 'ba' then '背泳ぎ'
                    when 'fly' then 'バタフライ'
                    when 'im' then '個人メドレー'
                    else style
                  end %>
                </th>
              <% end %>
            </tr>
            <tr>
              <% @events.each do |event| %>
                <th class="px-2 py-2 text-center font-medium text-gray-900 tracking-wider cursor-pointer hover:bg-gray-200"
                    style="background-color: <%= case event[:style]
                      when 'fr' then '#fde047'  # yellow-200
                      when 'br' then '#86efac'  # green-200
                      when 'ba' then '#fca5a5'  # red-200
                      when 'fly' then '#93c5fd'  # blue-200
                      when 'im' then '#d8b4fe'  # purple-200
                      else '#f3f4f6'  # gray-100
                    end %>"
                    data-sort-by="<%= event[:id] %>">
                  <%= "#{event[:distance]}m" %>
                  <% if @sort_by == event[:id] %>
                    <span class="ml-1">↑</span>
                  <% end %>
                </th>
              <% end %>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% if @sort_by.present? %>
              <% @male_players.each do |player| %>
                <tr class="hover:bg-gray-50 <%= 'bg-blue-100' if player.id == current_user_auth.user.id %>">
                  <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                    <%= player.generation %>期
                  </td>
                  <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                    <%= player.name %>
                  </td>
                  <% @events.each do |event| %>
                    <td class="px-2 py-2 whitespace-nowrap text-center"
                        style="background-color: <%= player.id == current_user_auth.user.id ? '#dbeafe' : case event[:style]
                          when 'fr' then '#fefce8'  # yellow-50
                          when 'br' then '#f0fdf4'  # green-50
                          when 'ba' then '#fef2f2'  # red-50
                          when 'fly' then '#eff6ff'  # blue-50
                          when 'im' then '#faf5ff'  # purple-50
                          else '#f3f4f6'  # gray-100
                        end %>">
                      <% time = @best_times[player.id][event[:id]] %>
                      <% if time %>
                        <%= format_time(time) %>
                      <% else %>
                        -
                      <% end %>
                    </td>
                  <% end %>
                </tr>
              <% end %>
            <% else %>
              <% @male_players_by_generation.each do |generation, players| %>
                <% players.each do |player| %>
                  <tr class="hover:bg-gray-50 <%= 'bg-blue-100' if player.id == current_user_auth.user.id %>">
                    <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                      <%= generation %>期
                    </td>
                    <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                      <%= player.name %>
                    </td>
                    <% @events.each do |event| %>
                      <td class="px-2 py-2 whitespace-nowrap text-center"
                          style="background-color: <%= player.id == current_user_auth.user.id ? '#dbeafe' : case event[:style]
                            when 'fr' then '#fefce8'  # yellow-50
                            when 'br' then '#f0fdf4'  # green-50
                            when 'ba' then '#fef2f2'  # red-50
                            when 'fly' then '#eff6ff'  # blue-50
                            when 'im' then '#faf5ff'  # purple-50
                            else '#f3f4f6'  # gray-100
                          end %>">
                        <% time = @best_times[player.id][event[:id]] %>
                        <% if time %>
                          <%= format_time(time) %>
                        <% else %>
                          -
                        <% end %>
                      </td>
                    <% end %>
                  </tr>
                <% end %>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>

    <div class="<%= @default_tab == 'female' ? '' : 'hidden' %> p-4 rounded-lg bg-gray-50" id="female" role="tabpanel" aria-labelledby="female-tab">
      <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200 text-xs">
          <thead>
            <tr>
              <th class="px-2 py-2 bg-gray-100 text-left font-medium text-gray-900 uppercase tracking-wider sticky left-0 z-10" rowspan="2">世代</th>
              <th class="px-2 py-2 bg-gray-100 text-left font-medium text-gray-900 uppercase tracking-wider sticky left-0 z-10" rowspan="2">選手名</th>
              <% @events.group_by { |e| e[:style] }.each do |style, events| %>
                <th class="px-2 py-2 text-center font-medium text-gray-900 uppercase tracking-wider" colspan="<%= events.size %>"
                    style="background-color: <%= case style
                      when 'fr' then '#fde047'  # yellow-200
                      when 'br' then '#86efac'  # green-200
                      when 'ba' then '#fca5a5'  # red-200
                      when 'fly' then '#93c5fd'  # blue-200
                      when 'im' then '#d8b4fe'  # purple-200
                      else '#f3f4f6'  # gray-100
                    end %>">
                  <%= case style
                    when 'fr' then '自由形'
                    when 'br' then '平泳ぎ'
                    when 'ba' then '背泳ぎ'
                    when 'fly' then 'バタフライ'
                    when 'im' then '個人メドレー'
                    else style
                  end %>
                </th>
              <% end %>
            </tr>
            <tr>
              <% @events.each do |event| %>
                <th class="px-2 py-2 text-center font-medium text-gray-900 tracking-wider cursor-pointer hover:bg-gray-200"
                    style="background-color: <%= case event[:style]
                      when 'fr' then '#fde047'  # yellow-200
                      when 'br' then '#86efac'  # green-200
                      when 'ba' then '#fca5a5'  # red-200
                      when 'fly' then '#93c5fd'  # blue-200
                      when 'im' then '#d8b4fe'  # purple-200
                      else '#f3f4f6'  # gray-100
                    end %>"
                    data-sort-by="<%= event[:id] %>">
                  <%= "#{event[:distance]}m" %>
                  <% if @sort_by == event[:id] %>
                    <span class="ml-1">↑</span>
                  <% end %>
                </th>
              <% end %>
            </tr>
          </thead>
          <tbody class="bg-white divide-y divide-gray-200">
            <% if @sort_by.present? %>
              <% @female_players.each do |player| %>
                <tr class="hover:bg-gray-50 <%= 'bg-blue-100' if player.id == current_user_auth.user.id %>">
                  <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                    <%= player.generation %>期
                  </td>
                  <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                    <%= player.name %>
                  </td>
                  <% @events.each do |event| %>
                    <td class="px-2 py-2 whitespace-nowrap text-center"
                        style="background-color: <%= player.id == current_user_auth.user.id ? '#dbeafe' : case event[:style]
                          when 'fr' then '#fefce8'  # yellow-50
                          when 'br' then '#f0fdf4'  # green-50
                          when 'ba' then '#fef2f2'  # red-50
                          when 'fly' then '#eff6ff'  # blue-50
                          when 'im' then '#faf5ff'  # purple-50
                          else '#f3f4f6'  # gray-100
                        end %>">
                      <% time = @best_times[player.id][event[:id]] %>
                      <% if time %>
                        <%= format_time(time) %>
                      <% else %>
                        -
                      <% end %>
                    </td>
                  <% end %>
                </tr>
              <% end %>
            <% else %>
              <% @female_players_by_generation.each do |generation, players| %>
                <% players.each do |player| %>
                  <tr class="hover:bg-gray-50 <%= 'bg-blue-100' if player.id == current_user_auth.user.id %>">
                    <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                      <%= generation %>期
                    </td>
                    <td class="px-2 py-2 whitespace-nowrap font-medium text-gray-900 sticky left-0 <%= player.id == current_user_auth.user.id ? 'bg-blue-100' : 'bg-white' %> z-10">
                      <%= player.name %>
                    </td>
                    <% @events.each do |event| %>
                      <td class="px-2 py-2 whitespace-nowrap text-center"
                          style="background-color: <%= player.id == current_user_auth.user.id ? '#dbeafe' : case event[:style]
                            when 'fr' then '#fefce8'  # yellow-50
                            when 'br' then '#f0fdf4'  # green-50
                            when 'ba' then '#fef2f2'  # red-50
                            when 'fly' then '#eff6ff'  # blue-50
                            when 'im' then '#faf5ff'  # purple-50
                            else '#f3f4f6'  # gray-100
                          end %>">
                        <% time = @best_times[player.id][event[:id]] %>
                        <% if time %>
                          <%= format_time(time) %>
                        <% else %>
                          -
                        <% end %>
                      </td>
                    <% end %>
                  </tr>
                <% end %>
              <% end %>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const tabs = document.querySelectorAll('[data-tabs-target]');
    const tabContents = document.querySelectorAll('[role="tabpanel"]');
    const sortHeaders = document.querySelectorAll('th[data-sort-by]');

    // タブ切り替え
    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const target = document.querySelector(tab.dataset.tabsTarget);
        const tabId = tab.id.replace('-tab', '');
        
        // Hide all tab contents
        tabContents.forEach(content => {
          content.classList.add('hidden');
        });
        
        // Show the selected tab content
        target.classList.remove('hidden');
        
        // Update tab styles
        tabs.forEach(t => {
          t.classList.remove('border-blue-600', 'text-blue-600', 'bg-blue-100');
          t.classList.add('border-transparent', 'hover:text-gray-600', 'hover:border-gray-300');
        });
        
        tab.classList.remove('border-transparent', 'hover:text-gray-600', 'hover:border-gray-300');
        tab.classList.add('border-blue-600', 'text-blue-600', 'bg-blue-100');

        // URLパラメータを更新
        const url = new URL(window.location.href);
        url.searchParams.set('tab', tabId);
        window.history.pushState({}, '', url);
      });
    });

    // 並び替え
    sortHeaders.forEach(header => {
      header.addEventListener('click', () => {
        const sortBy = header.dataset.sortBy;
        const currentSortBy = new URLSearchParams(window.location.search).get('sort_by');
        const currentTab = new URLSearchParams(window.location.search).get('tab') || '<%= @default_tab %>';
        
        // URLパラメータを更新
        const url = new URL(window.location.href);
        
        if (currentSortBy === sortBy) {
          // 同じヘッダーを再度クリックした場合は並び替えを解除
          url.searchParams.delete('sort_by');
        } else {
          // 新しいヘッダーをクリックした場合は並び替えを適用
          url.searchParams.set('sort_by', sortBy);
        }

        // 現在のタブを保持
        url.searchParams.set('tab', currentTab);
        
        window.location.href = url.toString();
      });
    });
  });
</script>
